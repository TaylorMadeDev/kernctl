name: build

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x
      - name: Restore
        run: dotnet restore Kernctl.sln
      - name: Format
        run: dotnet format Kernctl.sln --verify-no-changes --no-restore
      - name: Build
        run: dotnet build Kernctl.sln --configuration Release --no-restore
      - name: Test
        run: dotnet test Kernctl.sln --configuration Release --no-build
